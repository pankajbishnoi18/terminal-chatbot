It is a kind of mini LLM type chatbot , user can enter a keyword and it will respond if it have any info (already stored) on that keyword , it save the conversation 
history as json file and user can access the history anytime by using simple commands , user can also clear history.The core idea is json file handling, importing 
modules , session tracking modular functions and multi file project structure
